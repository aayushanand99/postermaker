export default function(state,action){
	// switch(action.type){
	// 	case "State responsible":return something//always return a fresh object
	// }
	return(state)//when the action is not related to the state taken care of
}